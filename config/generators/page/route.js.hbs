    {
      path: '{{ path }}',
      getComponent(location, callback) {
        require.ensure([], () => {
          const {{ properCase name }}Page = require(
            './pages/{{ properCase name }}Page'
        ).default;
          callback(null, {{ properCase name }}Page);
        });
      },
    },
$1
